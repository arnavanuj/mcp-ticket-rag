"""Repository markdown ingestion placeholder."""

from typing import Any


def ingest_repo_docs(owner: str, repo: str, max_docs: int) -> list[dict[str, Any]]:
    """Stub repo doc ingestor."""
    _ = (owner, repo, max_docs)
    return []
